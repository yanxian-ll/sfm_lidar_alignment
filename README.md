
1. 分别从激光点云和OBJ中将采样点云，方便用于CloudCompare手动配准和ICP处理；


2. 从OBJ中采样获得密集点云，为了避免渲染问题，将密集点云分块（重建点云）；


3. 将CLoudCompare获取的变换矩阵分别保存为 transform_manual.txt transform_icp.txt，
    然后将激光点云转换到粗对齐的SFM坐标系下；

4. 均匀选择部分下视角用于后续 Render-Lift 匹配优化，手动剔除掉不稳定的图像（例如大面积植被、水域、在激光点云覆盖范围内）；
    分别在选取的视角上，使用粗对齐的激光点云和重建点云渲染深度图和RGB；

5. Render-Lift 匹配（保留50张图像左右），使用sift提取同名点，每张图像最多保留 5 个匹配点，保证结果的可靠性，然后提升为三维对应点，
    计算全局变换矩阵。在这个基础上，执行 ICP（也只在选取的视角下）保证xxx。

6. 转换激光点云，然后融合激光点云和重建点云。

